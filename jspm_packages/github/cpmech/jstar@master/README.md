# Jstar JavaScript Tools

This package holds a number of utility functions and structures to develope JavaScript applications.

```bash
sudo npm install jspm -g
cd my-project
npm install jspm@beta --save-dev
jspm init
```
